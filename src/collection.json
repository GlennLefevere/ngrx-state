{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "init-state": {
      "description": "Adds a complete ngrx state setup to a module.",
      "factory": "./init-state/index",
      "schema": "./init-state/schema.json"
    },
    "init-effects": {
      "description": "Adds effects setup to existing state setup",
      "factory": "./init-effects/index",
      "schema": "./init-effects/schema.json"
    },
    "add-state": {
      "description": "Adds a state level to an existing state",
      "factory": "./add-state/index",
      "schema": "./add-state/schema.json"
    },
    "add-effect": {
      "description": "Adds an effect to an existing state build",
      "factory": "./add-effect/index",
      "schema": "./add-effect/schema.json"
    },
    "add-reducer": {
      "description": "Adds a reducer to closest combine reducer",
      "factory": "./add-reducer/index",
      "schema": "./add-reducer/schema.json"
    }
  }
}
