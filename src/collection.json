{
  "$schema": "../node_modules/@angular-devkit/schematics/collection-schema.json",
  "schematics": {
    "state": {
      "description": "Adds a complete ngrx state setup to a module.",
      "factory": "./state/index",
      "schema": "./state/schema.json"
    },
    "container-state": {
      "description": "Adds container state to an existing state",
      "factory": "./container-state/index",
      "schema": "./container-state/schema.json"
    },
    "data-state": {
      "description": "Adds data state to an existing state",
      "factory": "./data-state/index",
      "schema": "./data-state/schema.json"
    }
  }
}
